/*Recursion Form: http://actionsnippet.com/?p=2882*/var x = 10;var y = 10;Pen.lineStyle(1,0);function drawBox(iter, count, y, w){               if (count <iter){			   var width = w / count;			   for (var i = 0; i<count; i++){			   Pen.drawRect(i * width, width * count/w, width,width);			   }			   count++;			   drawBox(iter, count, y, width);                      }}drawBox(6,1,0,500);ScreenBuffer.draw(Canvas);